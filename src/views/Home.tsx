import Hero from '../components/Hero/Hero';

/**
 * Componente de la página de inicio.
 * Organiza y renderiza los componentes principales que conforman la página:
 * la animación de fondo, la barra de navegación, la sección de héroe y el pie de página.
 */
function Home() {
  return <Hero />;
}

export default Home; 