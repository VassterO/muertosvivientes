import styles from './Footer.module.css';

/**
 * Componente del pie de página.
 * Muestra una línea horizontal como cierre visual de la página.
 */
function Footer() {
  return <footer className={styles.footer}></footer>
}

export default Footer; 